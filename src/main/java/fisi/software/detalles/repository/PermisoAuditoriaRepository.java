package fisi.software.detalles.repository;

/**
 * Stub repository — auditing removed from permisos module.
 * This placeholder prevents compilation errors while references are cleaned.
 */
@Deprecated
public interface PermisoAuditoriaRepository {
    // intentionally empty
}
